package cardsharpers.utils;

import java.util.Random;

/**
 * Deck imitation.
 * Since the deck is infinite, it can be represented as
 * a randomly generated number in the range from 1 to 11.
 */
public final class RandomCard {
    public static final Random RND = new Random();
}
